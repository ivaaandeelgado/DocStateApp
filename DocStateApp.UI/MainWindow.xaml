<Window x:Class="DocStateApp.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocStateApp.UI"
        mc:Ignorable="d"
        Title="Gestión de documentos"
        Height="800"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize">

	<DockPanel LastChildFill="True">

		<StackPanel DockPanel.Dock="Left" Width="60" Background="#FF2D2D30">
			<!-- Icono de ajustes en la parte inferior -->
			<StackPanel VerticalAlignment="Bottom" Margin="0,0,0,20" >
				<Button Content="⚙" Width="40" Height="40"/>
			</StackPanel>
		</StackPanel>

		<DockPanel>

			<!-- Header superior -->
			<Border DockPanel.Dock="Top" Background="#FF007ACC" Height="50">
				<TextBlock Text="Gestión de documentos" Foreground="White" FontSize="20"
                           VerticalAlignment="Center" HorizontalAlignment="Center"/>
			</Border>

			<!-- Contenedor de columnas -->
			<Grid Background="#FFE5E5E5">
				<Grid.RowDefinitions>
					<RowDefinition Height="*" />
					<!-- Contenido principal -->
					<RowDefinition Height="100" />
					<!-- Footer fijo de 60px -->
				</Grid.RowDefinitions>


				<Grid Grid.Row="0">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>

					<!-- Columna To Do -->
					<Border Grid.Column="0"
					Margin="5"
					BorderBrush="DarkGray"
					BorderThickness="2"
					CornerRadius="5"
					Background="#FFF9F9F9">
						<StackPanel Grid.Column="0" Margin="10">
							<TextBlock Text="To Do" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
							<ItemsControl ItemsSource="{Binding ToDoFiles}">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<Border Background="White" Margin="0,0,0,10" Padding="10" CornerRadius="5"
												BorderBrush="Gray" BorderThickness="1">
											<TextBlock Text="{Binding Name}" />
										</Border>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</StackPanel>
					</Border>

					<!-- Columna In Progress -->

					<Border Grid.Column="1"
						Margin="5"
						BorderBrush="DarkGray"
						BorderThickness="2"
						CornerRadius="5"
						Background="#FFF9F9F9">
						<StackPanel Grid.Column="1" Margin="10">
							<TextBlock Text="In Progress" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
							<ItemsControl ItemsSource="{Binding InProgressFiles}">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<Border Background="Black" Margin="0,0,0,10" Padding="10" CornerRadius="5"
												 BorderBrush="Gray" BorderThickness="1">
											<TextBlock Text="{Binding Name}" />
										</Border>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</StackPanel>
					</Border>


					<!-- Columna Review -->


					<Border Grid.Column="2"
					Margin="5"
					BorderBrush="DarkGray"
					BorderThickness="2"
					CornerRadius="5"
					Background="#FFF9F9F9">
						<StackPanel Grid.Column="2" Margin="10">
							<TextBlock Text="Review" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
							<ItemsControl ItemsSource="{Binding ReviewFiles}">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<Border Background="White" Margin="0,0,0,10" Padding="10" CornerRadius="5"
												BorderBrush="Gray" BorderThickness="1">
											<TextBlock Text="{Binding Name}" />
										</Border>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</StackPanel>
					</Border>

					<!-- Columna Done -->

					<Border Grid.Column="3"
					Margin="5"
					BorderBrush="DarkGray"
					BorderThickness="2"
					CornerRadius="5"
					Background="#FFF9F9F9">
						<StackPanel Grid.Column="3" Margin="10">
							<TextBlock Text="Done" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
							<ItemsControl ItemsSource="{Binding DoneFiles}">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<Border Background="White" Margin="0,0,0,10" Padding="10" CornerRadius="5"
												BorderBrush="Gray" BorderThickness="1">
											<TextBlock Text="{Binding Name}" />
										</Border>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
						</StackPanel>
					</Border>

				</Grid>
				<Grid Grid.Row="1">
					<Border Grid.Column="0"
						Margin="5"
						BorderBrush="DarkGray"
						BorderThickness="2"
						CornerRadius="5"
						Background="#FFF9F9F9">
						<StackPanel Grid.Column="0" Margin="10">
							<Button Content="Analizar Documentos" Width="150" Height="50" 
									Command="{Binding TextoCommand}"/>
							
						</StackPanel>
					</Border>

				</Grid>
			</Grid>

		</DockPanel>
		
	</DockPanel>



		
</Window>
